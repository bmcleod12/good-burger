<li>
	{{burger}}
	
	{{#if sleep}}
	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">Devour it!
	</button>{{/if}}

	<button class="delete-burger" data-id="{{this.id}}">X</button>
</li>
